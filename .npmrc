# pnpm configuration only
auto-install-peers=true
strict-peer-dependencies=false
save-exact=true

# Note: node-version and pnpm-version have been removed to avoid npm warnings
# Use .nvmrc for Node version management and packageManager in package.json for pnpm version
